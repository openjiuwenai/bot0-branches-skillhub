# openJiuwen Agentic Hub 通信矩阵

本矩阵基于代码实际调用链梳理，记录 openJiuwen Agentic Hub 全产品（frontend、marketplace 后端、skill-runner、CLI）所有对外通信及产品入口。地址类配置项的默认值为代码内置，部署时可通过环境变量覆盖。矩阵中只包含默认值与配置变量名，不包含真实部署地址与凭证。构建期依赖镜像地址单独收录于第九节，产品运行时不存在对应连接。

## 一、外部公网服务（鉴权与 OAuth）

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | marketplace 后端 | 部署 IP | 动态 | GitCode | gitcode.com 解析 IP | 443 | HTTPS | GitCode 用户信息接口 | 是，`AUTH_USER_API_URL` 可配整个 URL | access_token query 参数 | TLS | 业务平面 | v0.1.0 | 默认 GitCode，可配自建鉴权服务；Bearer 鉴权默认启用 |
| 2 | 用户浏览器 | 终端用户 IP | 动态 | GitCode | gitcode.com 解析 IP | 443 | HTTPS | OAuth 授权页 | 是，`MARKET_GITCODE_OAUTH_AUTHORIZE_URL` | client_id query 参数 | TLS | 业务平面 | v0.1.0 | 浏览器跳转非后端出站；`MARKET_GITCODE_OAUTH_ENABLED=true` 时 |
| 3 | marketplace 后端 | 部署 IP | 动态 | GitCode | gitcode.com 解析 IP | 443 | HTTPS | OAuth 回调换 token | 是，`MARKET_GITCODE_OAUTH_TOKEN_URL` | client_id/client_secret POST body | TLS | 业务平面 | v0.1.0 | `MARKET_GITCODE_OAUTH_ENABLED=true` 时 |
| 4 | 用户浏览器 | 终端用户 IP | 动态 | GitHub | github.com 解析 IP | 443 | HTTPS | OAuth 授权页 | 是，`MARKET_GITHUB_OAUTH_AUTHORIZE_URL` | client_id query 参数 | TLS | 业务平面 | v0.1.0 | 浏览器跳转非后端出站；`MARKET_GITHUB_OAUTH_ENABLED=true` 时 |
| 5 | marketplace 后端 | 部署 IP | 动态 | GitHub | github.com 解析 IP | 443 | HTTPS | OAuth 回调换 token | 是，`MARKET_GITHUB_OAUTH_TOKEN_URL` | client_id/client_secret POST body | TLS | 业务平面 | v0.1.0 | `MARKET_GITHUB_OAUTH_ENABLED=true` 时 |
| 6 | marketplace 后端 | 部署 IP | 动态 | GitHub | api.github.com 解析 IP | 443 | HTTPS | GitHub 用户信息接口 | 是，`MARKET_GITHUB_AUTH_USER_API_URL` | Authorization: Bearer | TLS | 业务平面 | v0.1.0 | `MARKET_GITHUB_OAUTH_ENABLED=true` 时 |

## 二、基础设施（数据库 / 缓存 / 对象存储）

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 7 | marketplace 后端 | 部署 IP | 动态 | MySQL | 部署方指定 | 3306 | TCP/MySQL | 元数据持久化 | 是，`DB_PORT` | 用户名密码，连接串内 | 无 | 数据平面 | v0.1.0 | 必配 |
| 8 | marketplace 后端 | 部署 IP | 动态 | Redis | 部署方指定 | 6379 | TCP/RESP | OAuth state 暂存、会话跟踪、限流 | 是，`REDIS_PORT` | Redis 密码 | 无 | 数据平面 | v0.1.0 | `REDIS_HOST` 非空时启用 |
| 9a | marketplace 后端 | 部署 IP | 动态 | MinIO / OBS | 部署方指定 | 9000 / 443 | HTTP / HTTPS / S3 | 资产上传、索引读写、预签名 | 是，`MARKET_S3_ENDPOINT` 含端口 | AK/SK 或 OBS 临时凭证 | TLS（OBS）/ 无（MinIO 默认） | 数据平面 | v0.1.0 | 必配；MinIO 默认 HTTP，OBS HTTPS |
| 9b | 用户浏览器 / CLI | 终端用户 IP | 动态 | MinIO / OBS | 部署方指定 | 9000 / 443 | HTTP / HTTPS | 预签名 URL 直连下载资产 | 是，`MARKET_S3_ENDPOINT` 含端口 | 预签名 URL 签名凭证 | TLS（OBS）/ 无（MinIO） | 数据平面 | v0.1.0 | 下载时 |

## 三、LLM 与 Embedding 服务

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 10 | marketplace 后端 | 部署 IP | 动态 | LLM 服务 | 部署方指定 | 部署方指定 | HTTPS | 检索索引构建调用 LLM | 是，base_url 含端口 | Authorization: Bearer api_key | TLS | 业务平面 | v0.1.0 | `MARKET_RETRIEVAL_BUILD_METHOD=all` 时 |
| 11 | marketplace 后端 | 部署 IP | 动态 | Embedding 服务 | 部署方指定 | 部署方指定 | HTTPS | 索引构建生成 Embedding 向量 | 是，base_url 含端口 | Authorization: Bearer api_key | TLS | 业务平面 | v0.1.0 | `MARKET_RETRIEVAL_BUILD_METHOD` 含 embedding 时 |
| 12 | marketplace 后端 | 部署 IP | 动态 | LLM 服务 | 部署方指定 | 部署方指定 | HTTPS | Skill 发布审查调用 LLM | 是，base_url 含端口 | Authorization: Bearer api_key | TLS | 业务平面 | v0.1.0 | `MARKET_SKILL_REVIEW_ENABLED=true` 时 |
| 13 | marketplace 后端 | 部署 IP | 动态 | LLM 服务 | 部署方指定 | 部署方指定 | HTTPS | 非审查类 Skill 能力默认模型 | 是，base_url 含端口 | Authorization: Bearer api_key | TLS | 业务平面 | v0.1.0 | 按需 |

## 四、华为云服务（OBS dynamic 凭证模式）

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 14 | marketplace 后端 | 部署 IP | 动态 | 华为云 IAM | iam.myhuaweicloud.com 解析 IP | 443 | HTTPS | 换取 IAM Token、获取临时凭证 | 是，`HUAWEICLOUD_IAM_ENDPOINT` | 用户名密码换 X-Auth-Token | TLS | 管理平面 | v0.1.0 | `STORAGE_TYPE=OBS` 且 `MARKET_CREDENTIALS_MODE=dynamic` 时 |
| 15 | marketplace 后端 | 部署 IP | 动态 | 华为云 KMS | kms.{region}.myhuaweicloud.com 解析 IP | 443 | HTTPS | 密钥加解密 SecurityUtils | 是，`HUAWEICLOUD_KMS_ENDPOINT` | X-Auth-Token | TLS | 管理平面 | v0.1.0 | 同上 |

## 五、内部服务间通信

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 16 | marketplace 后端 | 部署 IP / Pod IP | 动态 | skill-runner | 部署方指定 / Pod IP | 8900 | HTTP | Playground 请求转发至 skill-runner | 是，`SKILL_RUNNER_URL` | 无 | 无 | 业务平面 | v0.1.0 | `PLAYGROUND_ENABLED=true` 时；K8s 下经 ClusterIP Service |
| 16b | frontend (nginx) | Pod IP | 动态 | marketplace 后端 | `skillhub-backend`（Service 名，同 namespace 解析） | 8100 | HTTP | 前端 nginx 将 `/api` 请求反向代理至后端 | 是，`BACKEND_URL`、`BACKEND_PORT` | 无 | 无 | 业务平面 | v0.1.0 | K8s 集群内部 ClusterIP 通信；服务名仅集群内 DNS 可解析，公网不可路由 |
| 17 | skill-runner | Pod IP | 动态 | K8s API Server | 集群 API 地址 | 6443 | HTTPS | Pod 生命周期管理、探活 | 否，K8s 集群固定 | ServiceAccount Token | TLS | 控制平面 | v0.1.0 | skill-runner 部署于 K8s 时 |
| 18 | skill-runner | Pod IP | 动态 | LLM 服务 | 部署方指定 | 部署方指定 | HTTPS | 转发 Playground 会话 LLM 请求 | 是，`SKILL_RUNNER_LLM_API_BASE` | Authorization: Bearer | TLS | 业务平面 | v0.1.0 | 真 LLM key 不进 Pod，经 llm_proxy 代理回连 |
| 19 | skill-runner | Pod IP | 动态 | Worker Pod | Worker Pod IP | 8080 | HTTP | provision Skill、run_turn、upload、reset | 是，`SKILL_RUNNER_POD_PORT` | Authorization: Bearer Worker Auth Token | 无 | 业务平面 | v0.1.0 | K8s executor 模式；Pod 网络内部，Worker 只监听 Pod IP |

## 六、动态地址通信（用户输入，含 SSRF 防护）

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 20 | marketplace 后端 | 部署 IP | 动态 | Git 仓库 | 用户配置 repo_url 解析 IP | 443 / 80 | HTTP(S) / Git | Git 源同步 fetch 仓库 | 否，由用户配置 repo_url 决定 | 公开仓库无认证 | TLS（HTTPS）/ 无（HTTP） | 业务平面 | v0.1.0 | `assert_public_git_clone_url` 强制公网 HTTP(S)，`assert_resolved_git_host_allowed` 校验主机；`GIT_TERMINAL_PROMPT=0` 禁用交互凭证 |
| 21 | marketplace 后端 | 部署 IP | 动态 | 上游 registry | download_url 解析 IP | 443 | HTTPS | ClawHub 兼容层下载 artifact | 否，运行时解析 | 无 | TLS | 业务平面 | v0.1.0 | `follow_redirects=True`，受 `MAX_FILE_SIZE` 限制 |

## 七、CLI 客户端通信

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 22 | CLI | 终端用户 IP | 动态 | marketplace 后端 | 部署方指定 | 8100 | HTTP | 发布、检索、下载元数据 | 是，`STORE_PORT` | Bearer Token | 无 | 业务平面 | v0.1.0 | CLI 使用时 |
| 23 | CLI | 终端用户 IP | 动态 | MinIO / OBS | 部署方指定 | 9000 / 443 | HTTP / HTTPS | 下载资产包 | 是，`MARKET_S3_ENDPOINT` 含端口 | 预签名 URL | TLS（OBS）/ 无（MinIO） | 业务平面 | v0.1.0 | 下载时 |

## 八、产品入口（用户访问）

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 24 | 用户浏览器 | 终端用户 IP | 动态 | frontend (nginx) | 部署方指定 / Pod IP | 9002 | HTTP | 产品 Web 入口：静态页面与 `/api` 反向代理 | 是，`FRONTEND_PORT` | 页面登录态由后端 Bearer Token 控制 | 无（生产环境建议由部署方在入口侧终止 TLS） | 业务平面 | v0.1.0 | K8s 下 Service 为 ClusterIP，对外暴露方式（Ingress / 端口映射）由部署方决定 |

## 九、构建期地址（仅镜像构建期，运行时无连接）

以下地址仅在容器镜像构建阶段用于依赖下载，产品交付后的运行时不存在对以下地址的任何连接，无需纳入运行时出网白名单。

| # | 源设备 | 源IP | 源端口 | 目的设备 | 目的IP | 目的端口（侦听） | 协议 | 端口说明 | 侦听端口是否可更改 | 认证方式 | 加密方式 | 所属平面 | 版本 | 特殊场景 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 25 | 镜像构建环境 | 构建主机 IP | 动态 | PyPI 镜像 | mirrors.aliyun.com 解析 IP | 443 | HTTPS | pip / uv Python 依赖下载镜像 | 是，构建参数 `INDEX_URL` / `PIP_INDEX` / `UV_INDEX_URL` / `TRUSTED_HOST` 覆盖 | 无 | TLS | 构建平面 | v0.1.0 | 仅构建期；`uv.lock` 默认 registry 为 pypi.org，构建时按镜像源覆盖 |
| 26 | 镜像构建环境 | 构建主机 IP | 动态 | apt 镜像 | mirrors.aliyun.com 解析 IP | 443 / 80 | HTTP(S) | 系统软件包下载镜像 | 是，构建参数 `APT_MIRROR` 覆盖 | 无 | TLS / 无 | 构建平面 | v0.1.0 | 仅构建期；默认源（deb.debian.org）不可达时启用 |

## 维护说明

- 本矩阵随代码调用链变更同步更新。新增对外通信或修改默认地址时，须同步修订对应条目。
- 矩阵中只记录默认值与配置变量名。真实部署地址、AK/SK、Token 等凭证属于部署记录，不得写入本文件。
- 部署方应根据本矩阵配置出网白名单：后端出站条目需在 marketplace / skill-runner 所在网络放行；浏览器跳转条目需在终端用户网络可达。
- "源端口"为动态出站端口，由操作系统分配，无需放行。"目的端口（侦听）"为对端监听端口，需在源端网络放行出站、目的端网络放行入站。
